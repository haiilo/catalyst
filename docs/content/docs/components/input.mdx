---
title: Input
status: not started
---

Inputs are used to allow users to provide text input when the expected input
is short. As well as plain text, Input supports various types of text,
including passwords and numbers.

## Examples

<LivePreview
  files={{
    "index.html": {
      code: `<cat-input label="Username"></cat-input>
<cat-input label="Email" type="email"></cat-input>
<cat-input label="Password" type="password"></cat-input>`
    },
    "styles.css": {
      code: `html, body {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100%;
  gap: 1rem;
}`
    }
  }}
/>

## Input Types

Use the `type` attribute to specify the input type.

<LivePreview
  files={{
    "index.html": {
      code: `<cat-input label="Text" type="text"></cat-input>
<cat-input label="Email" type="email"></cat-input>
<cat-input label="Password" type="password"></cat-input>
<cat-input label="Number" type="number"></cat-input>
<cat-input label="Search" type="search"></cat-input>`
    },
    "styles.css": {
      code: `html, body {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100%;
  gap: 1rem;
}`
    }
  }}
/>

## With Icons

Use the `icon` attribute to add an icon to the input.

<LivePreview
  files={{
    "index.html": {
      code: `<cat-input label="Search" icon="$cat:search"></cat-input>
<cat-input label="Email" icon="$cat:mail"></cat-input>`
    },
    "styles.css": {
      code: `html, body {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100%;
  gap: 1rem;
}`
    }
  }}
/>

## States

<LivePreview
  files={{
    "index.html": {
      code: `<cat-input label="Normal"></cat-input>
<cat-input label="Disabled" disabled></cat-input>
<cat-input label="Readonly" readonly value="Read only value"></cat-input>
<cat-input label="Invalid" invalid></cat-input>`
    },
    "styles.css": {
      code: `html, body {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100%;
  gap: 1rem;
}`
    }
  }}
/>

## With Hint

Use the `hint` attribute to provide additional context.

<LivePreview
  files={{
    "index.html": {
      code: `<cat-input 
  label="Password" 
  type="password"
  hint="Must be at least 8 characters long."
></cat-input>`
    },
    "styles.css": {
      code: `html, body {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100%;
  gap: 1rem;
}`
    }
  }}
/>

## Accessibility

- Always provide a label for the input
- Use the `required` attribute when the field is mandatory
- Provide clear error messages for invalid input

## API Reference

<include cwd>generated-api/input.md</include>
