scripts:
  installCommand: pnpm install --filter @haiilo/catalyst-docs --ignore-scripts --config.strict-peer-dependencies=false
  buildCommand: pnpm run build:docs --ignore-scripts --config.strict-peer-dependencies=false
  runCommand: pnpm run start:docs
  outputDirectory: ./docs/.next