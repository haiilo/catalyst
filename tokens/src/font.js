module.exports = {
  font: {
    family: {
      head: { value: "{asset.font.head.name.value}" },
      body: { value: "{asset.font.body.name.value}" },
      mono: { value: "{asset.font.mono.name.value}" }
    },
    weight: {
      head: { value: 600 },
      body: { value: 400 },
      mono: { value: "{font.weight.body.value}" }
    }
  }
}
