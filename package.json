{
  "name": "catalyst",
  "private": true,
  "scripts": {
    "prepare": "node ./prepare.js",
    "clean": "npm run clean -ws --if-present",
    "reset": "npm run reset -ws --if-present && rm -rf ./node_modules"
  },
  "workspaces": [
      "angular",
      "angular/projects/catalyst",
      "core",
      "react",
      "tokens",
      "vue"
    ],
  "publishConfig": {
    "workspaces": true
  },
  "engines": {
    "npm": ">=8.0.0",
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.2.1",
    "@commitlint/config-conventional": "^16.2.1",
    "husky": "^7.0.0"
  }
}
