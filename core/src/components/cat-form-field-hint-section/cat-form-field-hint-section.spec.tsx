/*
import { newSpecPage } from '@stencil/core/testing';
import { CatFormFieldHintSection } from '../cat-form-field-hint-section';

describe('cat-hint', () => {
  it('renders', async () => {
    const page = await newSpecPage({
      components: [CatFormFieldHintSection],
      html: `<CatFormFieldHintSection></CatFormFieldHintSection>`,
    });
    expect(page.root).toEqualHtml(`
      <cat-hint>
        <mock:shadow-root>
          <slot></slot>
        </mock:shadow-root>
      </cat-hint>
    `);
  });
});
*/
